#!/bin/sh


cd "$HOME"


file="$(dmenu -i -l 8 -p 'Config:' < "$HOME/.config/dmenu/dmenu_config")"
[ -z "$file" ] && exit


#st -e nano "$file" &
$(echo "st -e nano\nleafpad\ngeany" | dmenu) "$file" &
