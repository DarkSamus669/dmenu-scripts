#!/bin/sh


cd "$HOME"


file="$(dmenu -i -l 8 -p 'Config:' < "$HOME/.config/dmenu/dmenu_config")"
[ -z "$file" ] && exit


#editor="st -e nano"
editor="$(echo 'st -e nano\nleafpad\ngeany' | dmenu)"
[ -z "$editor" ] && exit

$editor "$file"
