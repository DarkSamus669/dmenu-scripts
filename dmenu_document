#!/bin/sh


cd "$HOME/Documents"


file="$(find -type f -not -path '*/.*' | sort | dmenu -i -l 8 -p 'Document:')"
[ -z "$file" ] && exit


$(echo "chromium\nqpdfview\nzathura\nleafpad" | dmenu) "$file" &
