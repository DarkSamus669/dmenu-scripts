#!/bin/sh


cd "$HOME/Documents"


file="$(find -type f -not -path '*/.*' | sort | dmenu -i -l 8 -p 'Document:')"
[ -z "$file" ] && exit


document_viewer="$(echo 'chromium\nqpdfview\nzathura\nleafpad' | dmenu)"
[ -z "$document_viewer" ] && exit

$document_viewer "$file" &
